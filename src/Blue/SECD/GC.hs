-- | Cheney two-space copying collector, reference version.  Ported to
-- an FSM mode in Blue.HW.GC (M5).
module Blue.SECD.GC (collect) where

collect :: a -> a
collect = error "Blue.SECD.GC.collect: TODO (M5)"
